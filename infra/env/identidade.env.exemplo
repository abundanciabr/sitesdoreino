# Célula: identidade — o login do site (DECISAO-celula-de-identidade)
# Copie para /opt/plataforma/env/identidade.env e preencha. O arquivo REAL é
# segredo, escrito à mão na VPS pelo mantenedor (INV-P8, Lei 5): nenhum valor
# de verdade entra neste exemplo, e nenhum agente vê o arquivo de lá.
#
# NÃO tem ALLOWED_HOSTS de propósito: esta célula fixa ALLOWED_HOSTS = ["*"] no
# config/settings.py e NÃO lê a variável — declarar aqui seria decoração que um
# dia alguém acredita (armadilhas/029, seção "de quebra").
#
# NÃO tem SCRIPT_NAME de propósito: os caminhos públicos (/entrar/…) são
# declarados por extenso no urls.py — o retorno do Google é o endereço EXATO
# cadastrado no console em 24/08/2026, sem prefixo de célula.

# ── Fail-hard no import: sem uma destas duas, o container não sobe. ──────────
DJANGO_SECRET_KEY=            # OBRIGATÓRIA — é ela que ASSINA o cookie do site
DATABASE_URL=postgres://identidade_user:TROQUE_identidade@postgres:5432/identidade_db

DEBUG=0

# ── Google: prova QUEM É ────────────────────────────────────────────────────
# O aplicativo OAuth é o MESMO da Caixa (criado pelo mantenedor em 24/08/2026,
# DECISAO-EVO-01 §6) — copie os dois valores de env/sugestoes.env. O endereço
# de retorno desta célula já está cadastrado nele desde aquele dia:
#   https://meshcraft.top/entrar/google/retorno
# Lidas no PONTO DE USO (apps/core/clients.py): faltando, quem fecha é só a
# porta de entrada — o container sobe e a API interna continua servindo.
GOOGLE_CLIENT_ID=TROQUE_id_do_cliente_do_google
GOOGLE_CLIENT_SECRET=TROQUE_segredo_do_cliente_do_google

# ── Quem é staff PARA EXIBIÇÃO (papel na resposta de /sessao) ───────────────
# Lista de e-mails separados por vírgula. O papel NÃO é gravado em lugar
# nenhum: é derivado desta lista a cada requisição. ATENÇÃO — este papel nunca
# autoriza nada (DECISAO-onde-mora-a-sessao §4): a lista que dá moderação da
# Caixa é a SUGESTOES_STAFF_EMAILS, no env DELA. Normalmente as duas têm o
# mesmo conteúdo, mas são decisões separadas de propósito.
IDENTIDADE_STAFF_EMAILS=voce@exemplo.com

# ── Tokens dos PARES consumidor→provedor (R1), um por par ───────────────────
# Quem pode PERGUNTAR "quem é o dono desta sessão?" (/interno/sessao):
TOKENS_ACEITOS_FUNIL=TROQUE_token_funil_para_identidade
TOKENS_ACEITOS_SUGESTOES=TROQUE_token_sugestoes_para_identidade
# A área administrativa (H21) — escrito pelo `infra/provisionar-admin.sh`:
TOKENS_ACEITOS_ADMIN=TROQUE_token_admin_para_identidade
# Quem, ALÉM de perguntar, pode receber o E-MAIL (/interno/sessao/completa) —
# só célula que autoriza recurso próprio sobre o e-mail entra aqui (a Caixa:
# matrícula e staff; a área admin: a lista ADMIN_EMAILS). O `funil` NÃO entra,
# por desenho (DECISAO §4). O valor é o MESMO token do par acima — o degrau é
# estar listado também aqui:
TOKENS_COMPLETOS_SUGESTOES=TROQUE_token_sugestoes_para_identidade
TOKENS_COMPLETOS_ADMIN=TROQUE_token_admin_para_identidade
#
# CADA PAR NOVO COM ACESSO AO E-MAIL SE REGISTRA POR ESCRITO na lei da
# identidade (DECISAO-celula-de-identidade §6.3) — e-mail é o dado que a EVO-01
# §3 concentrou numa linha, e cada par a mais alarga a superfície de dado
# pessoal. O do `admin` está registrado lá: a porta administrativa autoriza por
# lista de e-mails, e o e-mail é o único identificador estável que o mantenedor
# consegue gerir sozinho num env.

# O DEGRAU DE E-MAIL DO FUNIL (DECISAO-categorias-de-usuario §4, 28/08/2026).
# MESMO VALOR de TOKENS_ACEITOS_FUNIL acima — são dois degraus sobre o MESMO
# token, e valores diferentes dão 403 silencioso em toda página da home.
#
# Por que o funil precisa do e-mail: a categoria de uma pessoa é calculada por
# e-mail (é por e-mail que a `alunos` guarda matrícula), e não existe outro
# identificador comum entre as duas células. Registro por escrito exigido pela
# DECISAO-celula-de-identidade §6.3 — está no §4 daquela decisão.
TOKENS_COMPLETOS_FUNIL=TROQUE_token_funil_para_identidade

# O par alunos->identidade (29/08/2026, o aviso de liberacao). A celula que
# guarda os alunos conhece as pessoas por E-MAIL; a caixa de avisos entrega por
# ID DE PLATAFORMA, e so esta celula sabe traduzir um no outro.
#
# OS DOIS DEGRAUS SAO O MESMO VALOR, e isso nao e descuido: sao dois degraus
# sobre o MESMO token. O primeiro diz quem pode chamar (401 sem ele); o segundo
# libera a porta que procura por e-mail (403 sem ele), e ela exige o degrau a
# mais porque quem manda um e-mail para ela descobre se ele existe — existencia
# e informacao sobre uma pessoa. Valores DIFERENTES dariam 403 silencioso, e a
# carta simplesmente nunca sairia.
#
# NAO se escreve a mao: infra/provisionar-aviso-de-liberacao.sh gera e grava os
# dois lados, dentro da VPS, numa linha so.
TOKENS_ACEITOS_ALUNOS=TROQUE_token_alunos_para_identidade
TOKENS_COMPLETOS_ALUNOS=TROQUE_token_alunos_para_identidade

# O par cursos->identidade (05/09/2026, a sala de aula). A sala repassa o
# cookie opaco e pergunta QUEM é, pela resposta COMPLETA: é o e-mail que a
# `alunos` pede no caminho para dizer se a pessoa tem matrícula. O e-mail é
# usado nessa pergunta e descartado, nunca guardado nem exibido (o registro
# que o §6.3 exige está na DECISAO-celula-de-identidade, item `cursos`).
#
# OS DOIS DEGRAUS SÃO O MESMO VALOR. O primeiro nasce com o banco da sala
# (infra/provisionar-cursos.sh); o segundo, e a conferência de que os dois
# batem, é de infra/provisionar-pares-da-sala-de-aula.sh. Valores DIFERENTES
# dariam 403 silencioso, e a sala trataria todo mundo como visitante.
TOKENS_ACEITOS_CURSOS=TROQUE_token_cursos_para_identidade
TOKENS_COMPLETOS_CURSOS=TROQUE_token_cursos_para_identidade
