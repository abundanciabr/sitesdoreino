# =============================================================================
# ALARME DA MAIN — a rede embaixo do trapézio.
#
# Este repositório é privado numa conta pessoal, e o GitHub NÃO oferece required
# checks nesse plano (medido: `Upgrade to GitHub Pro or make this repository
# public`, HTTP 403). Ou seja: dá para mergear com tudo vermelho pelo site.
#
# `ci/mergear.py` fecha esse caminho quando o merge sai do terminal. Este
# workflow cobre o outro caminho — o clique no botão verde: se algo entrar na
# `main` e as invariantes do repositório quebrarem, ele ABRE UMA ISSUE em vez de
# deixar o vermelho só na aba Actions, onde ninguém olha.
#
# Ele NÃO é um portão: não impede nada, avisa depois. A distinção está escrita
# assim de propósito — chamar isto de proteção seria a mesma mentira que o
# INV-CI01 existe para eliminar. O modo de falha dele é "não avisou", nunca
# "aprovou o que não devia".
#
# O que ele varre, e o que NÃO varre, está medido no comentário longo do step
# "Muralha repo-wide" abaixo. Resumo: a guarda de segredos roda aqui (é a única
# das três muralhas que mede a árvore inteira, sem depender do contexto de um
# PR); as duas muralhas de diff ficam de fora POR ESCRITO, porque na `main` elas
# passariam por vacuidade ou reprovariam merges legítimos.
# =============================================================================
name: alarme-main
on:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  guardas-do-repositorio:
    name: guardas do repositório
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Dependências dos portões
        run: python -m pip install --quiet pytest pytest-xdist pyyaml
      # -----------------------------------------------------------------------
      # [INV-CI01] MURALHA REPO-WIDE NA `main` — e por que UMA das três, não as
      # três. Isto foi MEDIDO em 25/08/2026 contra a `main` do dia; a medição
      # crua está no corpo do PR que trouxe este step.
      #
      # (1) `ci/cerca-de-celula.sh` e `ci/orcamento-de-mudanca.sh` medem
      #     `git diff --name-only "${BASE_REF:-origin/main}"...HEAD`. Num push
      #     da `main`, HEAD *é* `origin/main`: o merge-base dos dois lados é o
      #     próprio HEAD, o diff sai VAZIO, e as duas imprimem
      #       `✅ Cerca de célula: OK — 0 célula(s) tocada(s)`   (exit 0)
      #       `Arquivos alterados: 0 … ✅ Orçamento de mudança: OK` (exit 0)
      #     Verde por VACUIDADE — a classe exata que o INV-CI01 existe para
      #     eliminar. Pô-las aqui assim trocaria "não rodou" por "rodou e não
      #     mediu nada", que é PIOR, porque parece proteção.
      #
      # (2) Dar-lhes uma base real (`github.event.before`) conserta o diff e não
      #     conserta o resto: as duas decidem por `PR_LABELS`, que não existe
      #     fora do evento `pull_request`. Medido no mesmo commit, mudando SÓ a
      #     variável:
      #       contracts/ tocado, PR_LABELS=contrato      → OK          (exit 0)
      #       contracts/ tocado, sem PR_LABELS           → ❌ MURALHA   (exit 1)
      #       17 arquivos, PR_LABELS=arquitetural        → OK          (exit 0)
      #       17 arquivos, sem PR_LABELS                 → ❌ ORÇAMENTO (exit 1)
      #     Ou seja: todo merge de contrato e toda mudança arquitetural
      #     LEGÍTIMOS — aprovados pelo rito, com a label no PR — abririam uma
      #     issue "main vermelha". Alarme que grita no caso certo é alarme que
      #     se aprende a ignorar; e aí ele deixa de servir para o caso errado.
      #     No fundo as duas são muralhas da FORMA DE UM PR (1 PR = 1 célula;
      #     orçamento POR PR) — e um push da `main` não é um PR: pode carregar
      #     vários de uma vez.
      #
      # (3) `ci/guarda-de-segredos.sh` não lê `BASE_REF`, não lê `PR_LABELS` e
      #     não usa `git diff`: varre a ÁRVORE INTEIRA com `git grep` e confere
      #     os placeholders `TROQUE_` dos modelos no disco. Na `main` ela mede
      #     exatamente o que mede num PR, e responde à pergunta que importa
      #     depois do fato — existe segredo alcançável na `main` AGORA? É a
      #     única das três com semântica idêntica num push, e é a que nunca
      #     tinha rodado aqui: até então a guarda de segredos jamais varreu a
      #     `main`. (Desde 26/08/2026 a `main` TEM required checks — `muralhas`
      #     e `ci-celula-gate`, ARMADILHAS-OPERACAO.md §1 H3 — o que reduz, mas
      #     não elimina, o valor deste alarme: ele mede a árvore inteira depois
      #     do fato, coisa que nenhum check de PR faz.)
      #
      # O SKIP das duas muralhas de diff é DECLARADO, não inferido (INV-CI01:
      # `SKIP` só existe quando alguém o declarou por escrito) — e é executável:
      # `ci/tests/test_alarme_main_roda_a_muralha_repo_wide.py` reprova se este
      # step sumir, reprova se alguém acrescentar aqui uma muralha de diff sem
      # antes refazer a medição, e reprova se a guarda de segredos deixar de ser
      # repo-wide (se ela passar a depender de diff, este step vira vácuo).
      # -----------------------------------------------------------------------
      - name: Muralha repo-wide — guarda de segredos (INV-P8)
        shell: bash
        run: |
          set -euo pipefail
          if [ ! -f ci/guarda-de-segredos.sh ]; then
            echo "❌ ERROR: ci/guarda-de-segredos.sh não existe."
            echo "   Portão ausente não é portão satisfeito. [INV-CI01]"
            exit 2
          fi
          # O script já separa os três estados: 0 OK · 1 segredo achado (FAIL) ·
          # 2 `git grep` não conseguiu varrer (ERROR). `set -e` propaga o exit
          # tal e qual, para o log distinguir "achei" de "não consegui olhar".
          bash ci/guarda-de-segredos.sh
      # A SUÍTE `ci/tests/` NÃO RODA MAIS AQUI (05/09/2026, alavanca 2 das
      # alavancas de 10x da fábrica, liberada pelo mantenedor). Ela já rodou no
      # PR, no job `muralhas`, sobre ESTE conteúdo: a `main` tem política
      # estrita, o PR só mergeia com a base em dia, e o que chega aqui é o que
      # o PR mediu. Rodar de novo era responder a mesma pergunta pela terceira
      # vez, e o portão de deploy esperava a resposta (1min18s por deploy).
      # A rede do Windows na `main` continua rodando a suíte na codepage dela,
      # por outro despacho da mesma data. O guarda desta decisão é
      # `ci/tests/test_alarme_main_roda_a_muralha_repo_wide.py`.

  alarme:
    name: alarme
    needs: guardas-do-repositorio
    if: failure()
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
    steps:
      - uses: actions/checkout@v4
      - name: Abrir (ou atualizar) a issue de main vermelha
        env:
          GH_TOKEN: ${{ github.token }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          SHA: ${{ github.sha }}
        shell: bash
        run: |
          set -euo pipefail
          CORPO="$(cat <<EOF
          As guardas de repositório falharam **na \`main\`**, depois de um merge.

          - commit: \`$SHA\`
          - execução: $RUN_URL

          ## O que isto significa

          A guarda de segredos varreu a árvore inteira da \`main\` e reprovou: ou
          achou um segredo alcançável (FAIL), ou não conseguiu varrer (ERROR).
          As duas coisas são diferentes, e a execução acima diz qual foi.

          Isto é um **alarme**, não um portão: ele avisa depois do fato. Ele nasceu
          quando a \`main\` não tinha required check nenhum (\`ARMADILHAS-OPERACAO.md §1 H3\`);
          desde 26/08/2026 ela tem, mas o alarme continua: os checks de PR olham o
          diff, e este aqui varre a árvore inteira JÁ na \`main\`.

          ## O que fazer

          1. Abra a execução acima e leia o estado (\`FAIL\` = violação, \`ERROR\` = não
             conseguiu medir — são coisas diferentes).
          2. Reproduza local: \`bash ci/guarda-de-segredos.sh\`
          3. Conserte por PR, como sempre. Se a \`main\` estiver inutilizável, reverter
             o merge é resposta legítima (RITOS.md §4: a resposta canônica é rollback).

          Feche esta issue quando a \`main\` voltar ao verde.
          EOF
          )"

          # Não empilhar issue: se já existe uma aberta, comenta nela.
          EXISTENTE="$(gh issue list --label main-vermelha --state open \
            --json number --jq '.[0].number // empty')"

          if [ -n "$EXISTENTE" ]; then
            echo "Issue #$EXISTENTE já aberta — comentando nela."
            gh issue comment "$EXISTENTE" --body "$CORPO"
          else
            # `|| true` aqui seria a mesma preguiça que o INV-CI01 combate: ele
            # esconderia "não consegui criar a label" junto com "ela já existe".
            # A pergunta é feita de forma explícita.
            if ! gh label list --json name --jq '.[].name' | grep -qx main-vermelha; then
              gh label create main-vermelha \
                --description "A main quebrou uma invariante do repositório" \
                --color b60205
            fi
            gh issue create \
              --title "main vermelha: guardas do repositório falharam em ${SHA:0:8}" \
              --label main-vermelha \
              --body "$CORPO"
          fi

  # ===========================================================================
  # A MAIN VERMELHA SE CURA PELO MESMO CAMINHO DE TODO MUNDO (Onda 6, P11)
  #
  # O alarme acima AVISA. Este job propõe a cura: abre um PR de reversão do
  # merge que quebrou, e pede pouso. Quem mergeia continua sendo a pista, com
  # todos os checks — inclusive os do próprio revert.
  #
  # POR QUE UM PR, E NÃO UM PUSH: a `main` tem conjunto de regras ativo (PR
  # obrigatório, sem force-push, dois checks obrigatórios) e ninguém escapa —
  # nem esta automação. Empurrar direto seria pedir 409 e ficar sem cura. Além
  # disso, um revert que quebra outra coisa precisa ser reprovado como qualquer
  # mudança: é o que os checks do PR fazem.
  #
  # A MIRA VEM ANTES DAS RECUSAS, e ela é MEDIDA (30/08/2026, TAR-025)
  # -------------------------------------------------------------------------
  # Até 30/08/2026 este job revertia `github.sha` — o commit do push da vez.
  # Numa `main` que JÁ estava vermelha isso acusa o inocente seguinte, e foi
  # exatamente o que aconteceu: o alarme concluiu `failure` OITO vezes entre
  # 12:13 e 12:31 UTC; o vermelho começou em `caaeb2e8` (PR #580) e as outras
  # sete execuções eram merges sem relação nenhuma com a quebra. Nas oito o job
  # rodou, e nas oito o commit de reversão já estava montado na árvore quando o
  # push bateu no 403 — na execução 33311082356 o diff local era
  # `15 files changed, 90 insertions(+), 1251 deletions(-)`, apagando a escrita
  # do fórum. Só a falta de permissão evitou oito PRs de reversão etiquetados
  # `pousar`, sete contra inocentes, mergeados sozinhos pela pista.
  #
  # `ci/mira_do_alarme.py` calcula o culpado do histórico do próprio workflow:
  # o commit da execução mais ANTIGA da sequência vermelha que chega até agora,
  # e essa sequência precisa começar logo depois de um `success`. Fora disso é
  # RECUSA — sem cura automática, com a issue chamando gente. Provado com
  # históricos montados à mão (inclusive o real deste incidente) em
  # `ci/tests/test_mira_do_alarme.py`, sem rede.
  #
  # Efeito colateral que vale mais que o principal: o ramo passa a se chamar
  # pelo CULPADO, então as oito execuções de uma sequência calculam o mesmo
  # nome e a recusa 3 as reduz a UM PR. "Um PR por incidente" só passou a ser
  # verdade agora.
  #
  # AS CINCO RECUSAS, todas fail-closed (nenhuma abre PR "por via das dúvidas"):
  #
  #   0. a mira não sabe quem quebrou -> não reverte ninguém. Janela toda
  #                                   vermelha, `cancelled` na fronteira, ou a
  #                                   `main` já verde: escolher alguém aqui é
  #                                   sortear a vítima.
  #   1. o commit não é merge      -> não reverte. `-m 1` num commit comum é
  #                                   erro, e adivinhar o pai é pior que parar.
  #   2. o commit JÁ é um revert   -> não reverte. Sem isto, um revert que
  #                                   também falhe geraria revert do revert, em
  #                                   laço, cada volta com um PR novo.
  #   3. já existe PR de reversão  -> não abre outro. Um PR por incidente.
  #      para este mesmo commit
  #   4. o revert dá conflito      -> não força. Conflito significa que a `main`
  #                                   andou por cima; resolver sozinho aqui é
  #                                   inventar código sem ninguém olhando.
  #
  # Em qualquer das cinco, o job termina VERDE e diz o motivo no log — o
  # alarme (a issue) já registrou o incidente, e este job é a tentativa de cura,
  # não o detector. Ficar vermelho aqui só duplicaria o vermelho de cima.
  #
  # O POUSO É CONDICIONAL, e a condição é medida. `PONTA=sim` significa que o
  # culpado é o commit mais novo que o alarme viu: reverter é "desfazer a
  # última coisa", a operação mais segura que existe em Git, e a etiqueta
  # `pousar` entra — a pista mergeia sozinha, como sempre. `PONTA=nao`
  # significa que outros merges já construíram por cima: o PR nasce pronto
  # (que é a parte cara) mas SEM a etiqueta, porque uma cirurgia no meio da
  # história não se mergeia sem ninguém ter olhado. Quem olhar pede pouso com
  # um comando; ninguém espera pelo mantenedor.
  #
  # USA `PISTA_TOKEN` pelo mesmo motivo da pista: PR aberto com o `GITHUB_TOKEN`
  # NÃO dispara os workflows de check, e um PR de reversão sem checks não
  # pousaria nunca.
  #
  # `contents: write` PORQUE FOI MEDIDO QUE FALTAVA. O `PISTA_TOKEN` vai para o
  # `gh`, mas quem empurra o ramo é o `git`, e a credencial que o
  # `actions/checkout` guarda é a do `GITHUB_TOKEN` — que aqui era `read`:
  #   remote: Permission to abundanciabr/sitesdoreino.git denied to github-actions[bot].
  #   fatal: ... The requested URL returned error: 403      (exit 128)
  # Ou seja, a cura automática nunca funcionou uma vez sequer, e ninguém sabia.
  # A escalada por job vale mesmo com o padrão do repositório em `read`
  # (`default_workflow_permissions: read`), e este repositório já a exerce em
  # três lugares que funcionam: `issues: write` aqui embaixo (abriu a issue
  # #587), `packages: write` no `deploy-celula` (publica no ghcr a cada deploy)
  # e `pull-requests: write` na `conferencia-do-toca`.
  # ===========================================================================
  reverter:
    name: propor reversão da main vermelha
    needs: guardas-do-repositorio
    if: failure()
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Propor a reversão (ou dizer por que não dá)
        env:
          GH_TOKEN: ${{ secrets.PISTA_TOKEN }}
          SHA: ${{ github.sha }}
          REPO: ${{ github.repository }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        shell: bash
        run: |
          set -euo pipefail

          if [ -z "${GH_TOKEN:-}" ]; then
            echo "PAROU: sem PISTA_TOKEN não dá para abrir um PR que rode checks."
            echo "A issue do alarme continua valendo; a reversão fica manual."
            exit 0
          fi

          # ----- recusa 0: quem quebrou? Se eu não souber, não escolho ninguém.
          MIRA=0
          SAIDA="$(python ci/mira_do_alarme.py --repo "$REPO" --sha "$SHA")" || MIRA=$?
          echo "$SAIDA"
          if [ "$MIRA" -ne 0 ]; then
            echo "--- A mira do socorro não apontou ninguém (exit $MIRA)."
            echo "Não reverto no escuro: em 30/08/2026 a mira antiga preparou"
            echo "sete reversões de commits inocentes. A issue do alarme continua"
            echo "valendo, e a cura fica com quem medir."
            exit 0
          fi
          ALVO="$(printf '%s\n' "$SAIDA" | sed -n 's/^MIRA=//p')"
          PONTA="$(printf '%s\n' "$SAIDA" | sed -n 's/^PONTA=//p')"
          if [ -z "$ALVO" ]; then
            echo "PAROU POR SEGURANÇA: a mira saiu 0 sem dizer o alvo."
            echo "Instrumento que não responde não vira permissão para agir."
            exit 0
          fi
          echo "--- Culpado medido: $ALVO (é a ponta da main? $PONTA)"

          PAIS="$(git rev-list --parents -n 1 "$ALVO" | wc -w)"
          if [ "$PAIS" -lt 3 ]; then
            echo "O commit $ALVO não é um merge (pais: $((PAIS - 1)))."
            echo 'Nao reverto: "-m 1" num commit comum e erro, e adivinhar o pai'
            echo "seria inventar. Conserte por PR, como sempre."
            exit 0
          fi

          TITULO="$(git log -1 --pretty=%s "$ALVO")"
          case "$TITULO" in
            Revert*|reverter:*)
              echo "O commit $ALVO JÁ é uma reversão ($TITULO)."
              echo "Não reverto de novo: seria um laço, cada volta com um PR novo."
              exit 0 ;;
          esac

          RAMO="reverter/${ALVO:0:12}"
          if gh pr list --state open --head "$RAMO" --json number --jq '.[0].number // empty' | grep -q .; then
            echo "Já existe PR de reversão aberto para $ALVO. Um por incidente."
            exit 0
          fi

          git config user.name "pista de pouso"
          git config user.email "pista@sitesdoreino.invalid"
          git switch -c "$RAMO"
          if ! git revert -m 1 --no-edit "$ALVO"; then
            # `|| true` aqui seria a preguiça que o INV-CI01 combate: ele
            # esconderia 'não havia o que abortar' junto com 'o abort
            # falhou'. A pergunta é feita, e a resposta é dita.
            if ! git revert --abort; then
              echo '(não havia revert em andamento para abortar — seguindo)'
            fi
            echo "O revert de $ALVO deu CONFLITO — a main andou por cima."
            echo "Não forço: resolver conflito sozinho aqui é inventar código"
            echo "sem ninguém olhando. A issue do alarme pede a mão humana."
            exit 0
          fi
          git push origin "$RAMO"

          if [ "$PONTA" = "sim" ]; then
            NOTA="A etiqueta \`pousar\` já está posta: **quem mergeia é a pista**, com
          todos os checks — inclusive os deste revert. Se o próprio revert reprovar,
          ele fica aqui parado, e aí é caso para mão humana."
          else
            NOTA="**Este PR NÃO pediu pouso, de propósito.** O commit culpado não é mais
          a ponta da \`main\` — outros merges entraram por cima dele, e reverter no meio
          da história é cirurgia, não 'desfazer a última coisa'. O trabalho caro já está
          feito; confira e, se for isso mesmo, peça pouso pelo caminho normal de todo
          mundo (\`RITOS.md\` §2 peça 4)."
          fi

          CORPO="$(cat <<EOF
          🔴 **A \`main\` ficou vermelha, e esta é a proposta de cura.**

          - commit revertido: \`$ALVO\` — $TITULO
          - execução que acusou: $RUN_URL
          - alvo escolhido por medição (\`ci/mira_do_alarme.py\`): é o commit da
            primeira execução vermelha do alarme depois do último verde — **não** o
            último merge a chegar.

          Reverter é a resposta canônica do \`RITOS.md\` §4: **primeiro apaga o fogo,
          depois investiga**. Este PR não conserta o defeito — ele devolve a \`main\`
          ao último estado verde conhecido, para que ninguém trabalhe em cima de
          uma base quebrada enquanto o conserto é escrito.

          $NOTA

          Se a correção de verdade for mais rápida que a reversão, feche este PR e
          mande a correção — a decisão é de quem estiver olhando.
          EOF
          )"
          gh pr create \
            --base main --head "$RAMO" \
            --title "reverter: $TITULO" \
            --body "$CORPO"
          gh pr edit "$RAMO" --add-label main-vermelha
          if [ "$PONTA" = "sim" ]; then
            gh pr edit "$RAMO" --add-label pousar
            echo "PR de reversão aberto e com pedido de pouso."
          else
            echo "PR de reversão aberto SEM pedido de pouso — o culpado não é a ponta."
          fi
