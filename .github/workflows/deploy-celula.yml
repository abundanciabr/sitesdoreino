# =============================================================================
# DEPLOY POR CÉLULA — merge na main ⇒ build da imagem SÓ da célula tocada ⇒
# pull+up SÓ dela na VPS. Rollback = *_TAG anterior (RITOS.md §4).
# O CI conhece apenas VPS_HOST e a chave de deploy. Segredos de RUNTIME
# (MP produção etc.) vivem SÓ em /opt/plataforma/env/*.env (INV-P8).
# =============================================================================
name: deploy-celula
on:
  push:
    branches: [main]
    paths: ['services/**']
permissions:
  contents: read
  packages: write
concurrency:
  group: deploy
  cancel-in-progress: false
jobs:
  detectar:
    runs-on: ubuntu-latest
    outputs:
      celulas: ${{ steps.d.outputs.celulas }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - id: d
        run: |
          C=$(git diff --name-only HEAD^...HEAD | grep '^services/' | cut -d/ -f2 | sort -u | jq -R . | jq -sc .)
          echo "celulas=$C" >> "$GITHUB_OUTPUT"
          echo "Células a deployar: $C"

  deploy:
    needs: detectar
    if: needs.detectar.outputs.celulas != '[]'
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 1
      matrix:
        celula: ${{ fromJSON(needs.detectar.outputs.celulas) }}
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build & push da imagem da célula
        run: |
          IMG=ghcr.io/abundanciabr/plataforma-${{ matrix.celula }}
          docker build -t "$IMG:${{ github.sha }}" -t "$IMG:main" "services/${{ matrix.celula }}"
          docker push "$IMG:${{ github.sha }}"
          docker push "$IMG:main"
      - name: Ativar na VPS (pull + up da célula E dos processos auxiliares dela)
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: deploy
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          script: |
            set -eu
            cd /opt/plataforma
            CELULA='${{ matrix.celula }}'
            # A célula não é mais UM container: os consumers de evento e o worker
            # Huey vivem em serviços "<celula>-<papel>" (infra/docker-compose.yml).
            # Subir só "<celula>" deixaria o auxiliar rodando a IMAGEM ANTIGA, em
            # silêncio — duas versões do mesmo código no ar, sem alarme nenhum.
            # A lista sai do PRÓPRIO compose, não de uma lista fixa aqui: a célula
            # que ganhar um auxiliar amanhã já entra sozinha, sem editar este arquivo.
            SERVICOS=$(docker compose config --services | grep -E "^${CELULA}(-|\$)" || true)
            if [ -z "$SERVICOS" ]; then
              echo "ERRO: '$CELULA' não tem serviço algum em /opt/plataforma/docker-compose.yml."
              echo "Abortado de propósito: 'up -d' sem argumento subiria a plataforma inteira."
              exit 1
            fi
            echo "Serviços desta célula: $SERVICOS"
            docker compose pull $SERVICOS
            # --wait reprova o deploy se algum container não ficar de pé. Sem ele,
            # 'ps' devolve sucesso com container em crash-loop (ARMADILHAS §3.13).
            docker compose up -d --wait --wait-timeout 180 $SERVICOS
            docker compose ps $SERVICOS
