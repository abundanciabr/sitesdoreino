AUTOMAÇÃO APOSENTADA. Não construa, inicie nem consulte um painel administrativo local.

A administração é acessada no site da VPS em `https://meshcraft.top/admin/`.
Os documentos ficam em `https://meshcraft.top/admin/documentos/`. Não execute
`ci/ligar_administracao.py` nem `abrir-a-administracao.cmd` para acessar o painel.

Se a rota não abrir depois do login, trate como falha de acesso ou publicação do
site e corrija pelo PR e pipeline. Não use localhost como alternativa. Este
arquivo fica apenas como registro da automação local encerrada.
