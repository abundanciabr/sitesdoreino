# Versões PINADAS e iguais às das células vizinhas de propósito: quatro versões
# da mesma biblioteca para o mesmo padrão são quatro comportamentos a depurar
# (`armadilhas/071`).
#
# Django 5.1.4 porque é a versão de todas as células vivas da plataforma.
Django==5.1.4
psycopg[binary]==3.2.3
dj-database-url==2.3.0
uvicorn==0.32.1
# A porta de leitura (degrau 7.4). Mesma versão das outras onze células que já
# expõem porta de máquina: o freeze de contrato compara o schema gerado byte a
# byte, e versões diferentes do django-ninja emitem OpenAPI diferente para o
# mesmo código.
django-ninja==1.3.0
# O transporte dos eventos nesta casa é Redis Streams, com grupo de consumo,
# PEL e fila morta (RECEITA R4 v1). Mesma versão das outras cinco consumidoras:
# divergir tornaria impossível comparar o comportamento de duas células no meio
# de um incidente.
redis==5.1.1
# O freeze de contrato le o congelado (contracts/metricas.openapi.yaml) e o
# compara byte a byte com o vivo, e o congelado e YAML. Faltou desde 04/09/2026,
# quando o contrato desta celula congelou (#1005) e a linha dela no manifesto
# virou `required`: o PR do Rito toca so contracts/, entao o ci-celula nem roda
# ali, e a cobranca so chega no PRIMEIRO PR seguinte que toque a celula. Foi
# este (#1089), cinco dias depois. Local passava por acidente, pelo PyYAML
# global da maquina. E a armadilhas/096, na sexta celula. Mesma versao das
# outras 12 com freeze required.
PyYAML==6.0.2
pytest==8.3.4
pytest-django==4.9.0
black==24.10.0
# NÃO há cliente HTTP aqui, e a ausência é a lei da célula: `AGENTS.metricas.md`
# proíbe consultar API de outra célula para completar um fato. O `httpx` e o
# `respx` que a gênese deixou plantados para "quando o cliente chegar" saíram no
# degrau 7.4, quando ficou claro que o cliente é a `admin` e mora lá. Esta
# célula só RECEBE (Redis Streams) e RESPONDE (django-ninja).
