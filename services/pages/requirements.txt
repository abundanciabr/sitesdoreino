# Versões PINADAS e iguais às das células vizinhas de propósito: quatro versões
# da mesma biblioteca para o mesmo padrão são quatro comportamentos a depurar
# (`armadilhas/071`).
#
# Django 5.1.4 porque é a versão de todas as células vivas da plataforma.
Django==5.1.4
# A porta de máquina do degrau 03. A versão é a MESMA das oito células vizinhas,
# e aqui a igualdade não é gosto: versões diferentes do django-ninja emitem
# OpenAPI diferente para o mesmo código, e o freeze de contrato compara os dois
# documentos byte a byte (`armadilhas/071`).
django-ninja==1.3.0
psycopg[binary]==3.2.3
dj-database-url==2.3.0
uvicorn==0.32.1
# A pergunta "quem é esta pessoa?" à célula `identidade` e "ela tem matrícula
# ativa?" à `alunos` — os clientes nascem no degrau 06 da escada, junto com a
# porta. Entra já aqui pelo mesmo motivo das versões pinadas: quando o cliente
# chegar, ele não escolhe versão nova.
httpx==0.28.1
pytest==8.3.4
pytest-django==4.9.0
black==24.10.0
# O dublê de rede dos testes: sem ele, a suíte precisaria de uma `identidade`
# de verdade no ar — e suíte que depende de outra célula fica vermelha por
# motivo alheio.
respx==0.23.1
# O freeze de contrato (`ci/contract_freeze.py`, via `make contrato-check`)
# importa PyYAML para comparar o congelado com o vivo. Ele DEVIA ter entrado no
# degrau 03, quando `contracts/pages.openapi.yaml` congelou e a linha desta
# célula no manifesto virou `required`, e não entrou. O buraco ficou invisível
# por dois dias porque nenhum PR tocou `services/pages/` desde então: a matriz
# do `ci-celula` só roda a célula que o diff toca, e a máquina de quem
# desenvolve tem PyYAML global, então o `make ci` local fica VERDE
# (`armadilhas/096`). Quem topou com ele foi o degrau 06, com a linha crua
# `contrato/pages ERROR PyYAML indisponível` e `make: *** [contrato-check]
# Error 2`. Versão igual à das vizinhas de propósito (`armadilhas/071`).
PyYAML==6.0.2
# A fila de fundo desta casa, pinada AQUI porque é este degrau que a usa pela
# primeira vez: a reconferência diária dos links das peças (critério AC-09,
# `apps/portfolio/tasks.py`). Foi o próprio degrau 02 que deixou escrito, neste
# arquivo, que o PR do degrau 08 as pinaria, e nas mesmas versões das oito
# células vizinhas — quatro versões da mesma biblioteca para o mesmo padrão são
# quatro comportamentos a depurar (`armadilhas/071`).
huey==2.5.1
redis==5.1.1
# Só de teste, e entra AGORA porque é o degrau 12 que faz esta célula falar: é
# com ele que `contracts/eventos/pages.portfolio.conferido.v1.json` deixa de ser
# documentação e passa a REPROVAR o CI. O envelope real é validado contra o
# arquivo do contrato congelado, nunca contra uma cópia do formato dentro do
# teste, que seria uma segunda verdade sobre o contrato. Versão igual à das
# vizinhas de propósito (`armadilhas/071`).
jsonschema==4.23.0
