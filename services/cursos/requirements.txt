# Versões PINADAS e iguais às das células vizinhas de propósito: quatro versões
# da mesma biblioteca para o mesmo padrão são quatro comportamentos a depurar
# (`armadilhas/071`).
#
# Django 5.1.4 porque é a versão de todas as células vivas da plataforma.
Django==5.1.4
psycopg[binary]==3.2.3
dj-database-url==2.3.0
uvicorn==0.32.1
# A pergunta "quem é esta pessoa?" à célula identidade (degrau 1.3) e "ela está
# matriculada?" à alunos (degrau 1.8) — os clientes nascem nesses degraus da
# escada. Entra já aqui pelo mesmo motivo das versões pinadas: quando o cliente
# chegar, ele não escolhe versão nova. O SDK da Anthropic do Assistente de
# laudo NÃO entra aqui: ele chega no degrau 2.3, na versão que o fórum já
# pinou, e nunca antes do botão que o usa.
httpx==0.28.1
# A porta de MÁQUINA da sala de aula (`/api/cursos`, degrau 1.3; o contrato
# congela no 1.4). Mesma versão das outras células de propósito
# (`armadilhas/071`).
django-ninja==1.3.0
pytest==8.3.4
pytest-django==4.9.0
black==24.10.0
# O dublê de rede dos testes: sem ele, a suíte precisaria de uma identidade de
# verdade no ar — e suíte que depende de outra célula fica vermelha por motivo
# alheio.
respx==0.23.1
