# Versões PINADAS e iguais às das células vizinhas de propósito: quatro versões
# da mesma biblioteca para o mesmo padrão são quatro comportamentos a depurar
# (`armadilhas/071`).
#
# Django 5.1.4 porque é a versão de todas as células vivas da plataforma.
Django==5.1.4
psycopg[binary]==3.2.3
dj-database-url==2.3.0
uvicorn==0.32.1
# A pergunta "quem é esta pessoa?" à célula identidade (degrau 1.3) e "ela está
# matriculada?" à alunos (degrau 1.8) — os clientes nascem nesses degraus da
# escada. Entra já aqui pelo mesmo motivo das versões pinadas: quando o cliente
# chegar, ele não escolhe versão nova.
httpx==0.28.1
# A porta de MÁQUINA da sala de aula (`/api/cursos`, degrau 1.3; o contrato
# congela no 1.4). Mesma versão das outras células de propósito
# (`armadilhas/071`).
django-ninja==1.3.0
# O Assistente de laudo (degrau 2.3): o SDK com que esta célula fala com um
# modelo de linguagem. A versão é a MESMA que o fórum já pinou, pelo motivo de
# sempre (`armadilhas/071`): duas versões da mesma biblioteca para o mesmo
# padrão são dois comportamentos a depurar. Ele instala junto o `httpx2`, um
# pacote DIFERENTE do `httpx` acima, e os dois convivem: é por isso que o corte
# de rede da suíte precisa cortar os dois (`armadilhas/288`).
anthropic==1.3.0
# O relay da outbox e o tique do prazo de revisão (degrau 2.1). As versões são
# as MESMAS de sugestoes/checkout/quiz/mensageria de propósito: um relay por
# célula em versão própria seria cinco comportamentos de biblioteca para o
# mesmo padrão (`armadilhas/071`).
redis==5.1.1
huey==2.5.1
pytest==8.3.4
pytest-django==4.9.0
black==24.10.0
# Só de teste: é com ele que `contracts/eventos/*.json` deixa de ser
# documentação e passa a reprovar o CI. O envelope real é validado contra o
# arquivo do contrato, não contra uma cópia do formato dentro do teste.
jsonschema==4.23.0
# O dublê de rede dos testes: sem ele, a suíte precisaria de uma identidade de
# verdade no ar — e suíte que depende de outra célula fica vermelha por motivo
# alheio.
respx==0.23.1
# O freeze de contrato (ci/contract_freeze.py, via `make contrato-check`)
# importa PyYAML para comparar o congelado com o vivo. Entrou no degrau 1.4,
# quando contracts/cursos.openapi.yaml congelou e a linha da célula no
# manifesto virou `required`: sem isto o CI da célula fica ERROR "PyYAML
# indisponível", e local fica verde porque a máquina de quem desenvolve tem
# PyYAML global (`armadilhas/096`).
PyYAML==6.0.2
