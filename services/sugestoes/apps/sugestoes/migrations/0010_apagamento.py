# Generated by hand, no formato dos que a assinam nesta pasta.
"""O apagamento definitivo — `docs/decisoes/DECISAO-apagar-ideia.md`.

Duas colunas, no mesmo desenho de `0009_arquivamento`: `apagada_em` (o
carimbo) e `apagada_por` (`PROTECT`, como toda referência a `Identidade`
nesta célula). Uma ideia apagada não tem a LINHA removida — só o conteúdo
(título, texto, votos, comentários) — porque `HistoricoStatus`,
`ChangeSpecAprovado` e `Aviso` são `PROTECT`/append-only e apontam para ela.
"""

import django.db.models.deletion
from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ("sugestoes", "0009_arquivamento"),
    ]

    operations = [
        migrations.AddField(
            model_name="sugestao",
            name="apagada_em",
            field=models.DateTimeField(blank=True, null=True),
        ),
        migrations.AddField(
            model_name="sugestao",
            name="apagada_por",
            field=models.ForeignKey(
                blank=True,
                null=True,
                on_delete=django.db.models.deletion.PROTECT,
                related_name="ideias_apagadas",
                to="sugestoes.identidade",
            ),
        ),
    ]
