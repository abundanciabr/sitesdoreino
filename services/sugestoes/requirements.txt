Django==5.1.4
# A superfície de MÁQUINA da Caixa (DECISAO-onde-mora-a-sessao): o `funil`
# pergunta "quem é o dono desta sessão?". Mesma versão EXATA de alunos e
# checkout de propósito — quatro versões da mesma biblioteca para o mesmo
# padrão são quatro comportamentos a depurar (`armadilhas/071`), e o freeze de
# contrato compara o schema que ELA gera.
django-ninja==1.3.0
psycopg[binary]==3.2.3
dj-database-url==2.3.0
uvicorn==0.32.1
# Cliente das duas conversas de fora (Google e alunos) — R2, timeout explícito.
httpx==0.28.1
# O relay da outbox (EVO-20). As versões são as MESMAS de checkout/quiz/
# mensageria de propósito: um relay por célula em versão própria seria quatro
# comportamentos de biblioteca para o mesmo padrão (`armadilhas/071` mostra o
# preço de divergir — o freeze da raiz roda com o Python global da máquina).
redis==5.1.1
huey==2.5.1
pytest==8.3.4
pytest-django==4.9.0
# Só de teste: é com ele que `contracts/eventos/*.json` deixa de ser
# documentação e passa a reprovar o CI — o envelope real é validado contra o
# arquivo do contrato, não contra uma cópia do formato dentro do teste.
jsonschema==4.23.0
# O freeze de contrato (make contrato-check) LÊ o contrato congelado, que é
# YAML. Faltou desde o #139 (a célula virou freeze:required num PR que não
# tocava services/, então o ci-celula nunca rodou o freeze dela no runner) e
# só apareceu no primeiro PR da célula depois do congelamento — local ficava
# verde pelo PyYAML global da máquina. Mesma versão das outras 6 células.
PyYAML==6.0.2
# O dublê de rede dos testes. Sem ele a suíte precisaria de internet, e uma
# suíte que depende do Google é uma suíte que fica vermelha por motivo alheio.
respx==0.23.1
black==24.10.0
