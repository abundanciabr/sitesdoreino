# Versões PINADAS e iguais às das células vizinhas de propósito: quatro versões
# da mesma biblioteca para o mesmo padrão são quatro comportamentos a depurar
# (`armadilhas/071`).
#
# Django 5.1.4 (e não 5.0.x) porque é a versão das células nascidas depois de
# 22/08 — `funil`, `quiz`, `mensageria`, `sugestoes`, `identidade`.
Django==5.1.4
psycopg[binary]==3.2.3
dj-database-url==2.3.0
uvicorn==0.32.1
# A pergunta "quem e esta pessoa?" a celula identidade (R2). Mesma versao
# das outras celulas de proposito (`armadilhas/071`).
httpx==0.28.1
pytest==8.3.4
pytest-django==4.9.0
black==24.10.0
# O duble de rede dos testes da porta: sem ele, a suite precisaria de uma
# identidade de verdade no ar — e suite que depende de outra celula fica
# vermelha por motivo alheio.
respx==0.23.1
# A porta de MAQUINA (`config/api.py`). Mesma versao das celulas vizinhas de
# proposito: versoes diferentes do django-ninja emitem OpenAPI com diferencas
# cosmeticas, e o freeze de contrato compara byte a byte (`armadilhas/071`).
django-ninja==1.3.0
# O freeze de contrato importa PyYAML para comparar o congelado com o vivo. Ele
# entra AQUI, no mesmo PR em que a celula passa a ter contrato: sem esta linha,
# o `contrato/admin` termina em `ERROR PyYAML indisponivel` no runner, enquanto
# o `make ci` local fica VERDE porque a maquina de quem desenvolve tem PyYAML
# global (`armadilhas/096`, a mesma que a `pages` topou).
PyYAML==6.0.2
