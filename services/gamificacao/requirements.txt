# Versões PINADAS e iguais às das células vizinhas de propósito: quatro versões
# da mesma biblioteca para o mesmo padrão são quatro comportamentos a depurar
# (`armadilhas/071`).
#
# Django 5.1.4 porque é a versão de todas as células vivas da plataforma.
Django==5.1.4
psycopg[binary]==3.2.3
dj-database-url==2.3.0
uvicorn==0.32.1
# A pergunta "quem é esta pessoa?" à célula identidade. O cliente chegou antes
# do previsto: quem o usa é `getMyStatus` da porta de máquina
# (`apps/core/sessao.py`), não a primeira tela. Mesma versão das outras células
# de propósito (`armadilhas/071`).
httpx==0.28.1
# A porta de MAQUINA da gamificacao (`/api/gamificacao`,
# `contracts/gamificacao.openapi.yaml`). Mesma versao das outras celulas de
# proposito (`armadilhas/071`).
django-ninja==1.3.0
# O consumidor de eventos (Redis Streams, receita R4 v1). MESMA versão das
# outras quatro células consumidoras de propósito: quatro versões da mesma
# biblioteca para o mesmo padrão são quatro comportamentos a depurar
# (`armadilhas/071`).
redis==5.1.1
# O freeze de contrato importa PyYAML para comparar o congelado com o vivo. Ele
# entra NO MESMO DIA em que esta celula vira mensuravel, e nao depois: sem ele o
# `make contrato-check` morre com "No module named 'yaml'" no primeiro PR
# seguinte, e local fica verde porque a maquina de quem desenvolve tem PyYAML
# global (`armadilhas/096`).
PyYAML==6.0.2
pytest==8.3.4
pytest-django==4.9.0
black==24.10.0
# O dublê de rede dos testes: sem ele, a suíte precisaria de uma identidade de
# verdade no ar — e suíte que depende de outra célula fica vermelha por motivo
# alheio.
respx==0.23.1
