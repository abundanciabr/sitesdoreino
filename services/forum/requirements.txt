# Versões PINADAS e iguais às das células vizinhas de propósito: quatro versões
# da mesma biblioteca para o mesmo padrão são quatro comportamentos a depurar
# (`armadilhas/071`).
#
# Django 5.1.4 porque é a versão de todas as células vivas da plataforma. Vale
# registrar por que isto importa nesta célula em particular: a rodada de
# consultoria descartou o motor pronto `django-machina` justamente por ele não
# declarar suporte a esta versão (última release estável de outubro de 2023).
# Ver `docs/consultorias/forum-da-escola/VEREDITO.md`.
Django==5.1.4
psycopg[binary]==3.2.3
dj-database-url==2.3.0
uvicorn==0.32.1
# A pergunta "quem é esta pessoa?" à célula identidade. Mesma versão das outras
# células de propósito (`armadilhas/071`).
httpx==0.28.1
# A porta de MAQUINA do forum (`/interno`, contracts/forum.openapi.yaml). Mesma
# versao das outras celulas de proposito (`armadilhas/071`).
django-ninja==1.3.0
# O freeze de contrato importa PyYAML para comparar o congelado com o vivo. Ele
# entra NO MESMO DIA em que o manifesto desta celula vira `required`, e nao
# depois: sem ele o `make contrato-check` morre com "No module named 'yaml'" no
# primeiro PR seguinte que tocar a celula, e local fica verde porque a maquina
# de quem desenvolve tem PyYAML global (`armadilhas/096`).
PyYAML==6.0.2
pytest==8.3.4
pytest-django==4.9.0
black==24.10.0
# O dublê de rede dos testes: sem ele, a suíte precisaria de uma identidade de
# verdade no ar — e suíte que depende de outra célula fica vermelha por motivo
# alheio.
respx==0.23.1

# A VOZ do forum (01/09/2026, degrau 17): a outbox e o relay que levam os quatro
# eventos ao fio. MESMAS versoes das outras celulas de proposito — um relay por
# celula em versao propria seria N comportamentos de biblioteca para o mesmo
# padrao (`armadilhas/071`).
huey==2.5.1
redis==5.1.1
# So de teste: e com ele que os contratos congelados de contracts/eventos/forum.*
# deixam de ser documentacao e passam a reprovar o CI — o envelope real e
# validado contra o ARQUIVO, nunca contra uma copia do formato dentro do teste.
jsonschema==4.23.0
